static int[] arr;

public static int fibo(int n){
    if(arr[n-1] > 0){
        return arr[n-1];
    }
    if(n==1 || n==2){
        return arr[n-1] = 1;
    }
    return arr[n-1] = fibo(n-1)+fibo(n-2);
}


//반복문
for(int i=2; i<n; i++){
    c=a+b;
    System.out.print(c+" ");
    a=b;
    b=c;
}