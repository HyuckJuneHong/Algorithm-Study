<효율성 O(n^2) -> O(n)>

//최대 매출 - 중첩 반복 O(n^2)
int max = Integer.MIN_VALUE;
for(int i=0; i<n-k; i++){
    int sum = 0;
    for(int j=i; j<i+k ; j++){
        sum += arr[j];
    }
    max = Math.max(max, sum);
}

//최대 매출 - 반복 O(n)
int max = Integer.MIN_VALUE;
for(int i=0; i<k; i++)
    sum += arr[i];
for(int i=k; i<n; i++){
    sum += (arr[i] - arr[i-k]);
    max = Math.max(max, sum);
}


